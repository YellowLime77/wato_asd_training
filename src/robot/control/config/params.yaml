control_node:
  ros__parameters:
    lookahead_distance: 1.5  # m
    goal_tolerance: 0.3      # m, keep this smaller than the planner's goal_tolerance
    linear_speed: 0.8        # m/s
    max_angular_speed: 1.0   # rad/s
